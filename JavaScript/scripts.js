let idade = 5;
console.log(idade);
let altura = 180;
const inalteravel = "sempre";
console.log(altura);

// let = variável que pode alterar a qualquer momento
// const = Constante, uma definição que não pode ser alterado.
// string literal = qualquer atribuição de variável que contenha letras dentro de ""
// number literal = qualquer numero dentro de variável
// object = definição de várias variaveis separados por , dentro de um {}
// boolean = regras do sistema (true,false, etc..)
// undefind = variavel sem conteudo.
// null = redefinir valor..
// tipagem dinâmica = o javascript tem identificador automatico e pode ser alterado a qualquer momento
// operadores aritiméticos = +, -, / , *, **(expoente), %(resto que sobrou de uma divisão);


// operadores de incremento = ++, -- incrementam ou diminuem o numero atribuido por 1 
// e a ordem em que coloca ele reflete no resultado. sendo antes o resultado imediato e 
// depois na segunda vez que realizar o console.log

// -= e += pegam o valor e aumentam ele mesmo.
// && e || funcionam como AND e OR pra fazer operações

// comparação ? atribui valor para uma condição. exemplo (n<18)? "Too young":"Too old" no caso o ? 
//atribui os valores  "Too Young" se for menor e "Too old" se for maior.

// if/else para realizar comandos com resultados diferentes no padrão if (uma regra) {
//   acontecimento 
// } else {
// outro resultado    
//}

//switch / case = forma de trocar uma constatação de acordo com o resultado. exemplo:
// (fruits) {
//case
//"Banana":
//  alert("Hello")
//   break;

//case
//"Apple":
//   alert("Welcome")
//   break;
// }
// default: é no caso de não dar retorno em nenhum dos casos acima e vai após o ultimo "break"


// Arrays são utilizados para armazenar múltiplos valores em uma única variável.

//nomeDoArray.pop() = remove o ultimo item do array
//nomeDoArray.push() = adiciona o que está no parentese pra dentro do array
//nomeDoArray.splice() = remover o que está no parentese do array (posição,quantidade ate final pra remover);
//nomeDoArray.sort() = organizar o que está no array
//nomeDoArray.shift(); = remove o inicio do array
//nomeDoArray.unshift(); = adicionar ao inicio do array
// nomeDoArray[0] = adicionar novo item primeiro dentro do array
// nomeDoArray.indexOf('nomevariavel') = procura numero de indice dentro do Array
//nomeDoArray.slice(); = copiar o que está no array
